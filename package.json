{
  "name": "relaywash",
  "version": "0.1.0",
  "description": "Claude Code plugin: clean agent tool output, lower token burn",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "build": "node build/build.mjs",
    "test": "node --test --test-reporter=spec test/*.test.js"
  }
}
