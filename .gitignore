node_modules/
.relayburn/
*.log
.DS_Store
# burn-compare local output (CI writes a different copy as a job artifact)
compare-result.json
.npm-cache
# Claude Code agent worktrees
.claude/
# Rust build output
target/
Cargo.lock
# Per-platform binary payloads — produced by CI on publish, never committed.
packages/*/bin/wash
packages/*/bin/wash.exe
